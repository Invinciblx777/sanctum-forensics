# Fallback media

Nothing in this directory is generated by `demo-reset.sh`. It has to be recorded
by hand, once, and re-recorded whenever the UI changes shape. **Record it during
a rehearsal that went well** — a fallback captured while fixing something is a
fallback that shows the thing being fixed.

`docs/demo/runbook.md` names each of these by path. If a file here is missing,
that beat has no fallback and the runbook's "if it fails" row is a lie.

---

## What each beat needs, concretely

Six beats. For each: the screen recording or image, the artefact that has to be
on disk, and the command to fall back to when the UI does not come up.

### 0:00 — Devices

| | |
|---|---|
| **Capture** | `devices.png`. Still image, full browser window, not a crop. The Devices screen with the stick listed, the `CLEAR ONLY` badge visible, **and the badge tooltip open** showing its evidence string. The tooltip is the beat; a screenshot without it is a screenshot of a table. |
| **Staged artefact** | none — this beat reads live from `GET /devices`. |
| **Command fallback** | `.venv/bin/python -c "from core.device.enumerate import enumerate_devices; import json; print(json.dumps([d.model_dump(mode='json') for d in enumerate_devices()], indent=2))"` — prints the same probe output the screen renders, capability evidence included. Verified to run unprivileged on the validation host. |

### 0:45 — Start the wipe

| | |
|---|---|
| **Capture** | `wipe-start.mp4`. **50 seconds**, screen capture, no audio — you narrate live over it. From clicking **Erase this device** through to the ERASE progress bar moving. Must contain: the confirm dialog, `PREFLIGHT`/"calibrating write", the plan panel filling in, `CONTROLLER_WRITE_ELISION` appearing at HIGH, and the Residual risk panel showing level **high** with four factors. If the capture misses the finding, it is not a fallback for this beat. |
| **Staged artefact** | `docs/validation/results-20260905T033655Z/a4-erase.json` — already in the repo. Carries the real plan, the real `fill_reason`, and the real elision finding. |
| **Command fallback** | `jq '.result.plan, (.result.residual_risk.findings[] \| select(.kind=="CONTROLLER_WRITE_ELISION"))' docs/validation/results-20260905T033655Z/a4-erase.json` — findings live under `residual_risk` and the identifier field is `kind`, not `code`. |

### 1:30 — PhotoRec, before and after

| | |
|---|---|
| **Capture** | none needed. This beat is already three terminal commands reading JSON; there is no UI to lose. |
| **Staged artefact** | `/var/lib/sanctum-demo/demo/photorec-before.json`, `photorec-after.json`, `planted-match.txt` — written by `--full`. |
| **Command fallback** | the same three commands against the repo copies: `jq -r '.files_recovered' docs/validation/results-20260905T033655Z/photorec-before.json` and `…/photorec-after.json`. Say "these are from the hardware validation run on the fifth of September". |

### 2:15 — Recovery and the score breakdown

| | |
|---|---|
| **Capture** | `recovery.png`. The Recovery screen filtered to HIGH, one candidate selected, the Score breakdown panel open with **all six components visible** and their evidence strings readable at projector distance. |
| **Staged artefact** | `/var/lib/sanctum-demo/demo/recovery-fat32.dd` plus `recovery-manifest-fat32.json`. Second fallback, already in the repo: `docs/performance/calibration.md`'s per-filesystem table. |
| **Command fallback** | `.venv/bin/python scripts/hardware_validation.py carve --image /var/lib/sanctum-demo/demo/recovery-fat32.dd --manifest /var/lib/sanctum-demo/demo/recovery-manifest-fat32.json --filesystem fat32 --damage delete` — prints the buckets, the candidate count and the recall as JSON. Note `--damage`: it is required for the comparison to pick the right baseline. |

### 3:15 — Sign, tamper, restore

| | |
|---|---|
| **Capture** | none. This beat is terminal-only by design and is stronger for it. |
| **Staged artefact** | **two, and they must come from the same reset**: `demo-erase.forensic.json` and `demo-ledger/`. Copy both together or two of the five checks go SKIP. |
| **Command fallback** | the three runbook commands with `--ledger-root docs/demo/fallback/demo-ledger` substituted. |

```bash
cp /var/lib/sanctum-demo/reports/demo-erase.forensic.json docs/demo/fallback/
cp -a /var/lib/sanctum-demo/ledger docs/demo/fallback/demo-ledger
```

### 4:15 — Nine defects

| | |
|---|---|
| **Capture** | none. Slide only. |
| **Staged artefact** | `docs/validation/hardware.md` — in the repo. The three-run table on the slide is drawn from it. |
| **Command fallback** | none needed; if the slide deck is gone, read the table out of `hardware.md`. |

---

## Which of these the run currently staging can produce

`--full` writes the artefacts. It does **not** write a single capture — every
`.png` and `.mp4` here needs a person at the keyboard with a recorder running.

**Available the moment staging finishes, no extra session:**

- `demo-erase.forensic.json` and `demo-ledger/` — the two `cp` commands above.
  Do this first; it is the only fallback that protects the strongest beat.
- Every staged JSON the 1:30 and 2:15 command fallbacks read. Already on disk.

**Needs a separate capture session, with the server up and the browser open:**

- `devices.png` — needs the UI and the tooltip open.
- `recovery.png` — needs a completed carve on screen with a candidate selected.
- `wipe-start.mp4` — **needs a real wipe to start**, which means 50 seconds of a
  destructive run. Capture it during a rehearsal you were going to run anyway;
  do not start a wipe solely to film it.

Because `wipe-start.mp4` costs a wipe, capture it during **rehearsal 1**, not
before it: the first rehearsal already begins with a live wipe at 0:45, and
recording that one costs nothing extra.

## Verify the report fallback still works

Run this after copying it, and again before every rehearsal:

```bash
SANCTUM_KEY_PASSPHRASE=sanctum-demo .venv/bin/python -m core.report.cli \
    verify-report docs/demo/fallback/demo-erase.forensic.json \
    --ledger-root docs/demo/fallback/demo-ledger
```

Five `[PASS]` lines and `Result: PASS`, then a `Note:` line about identity that
always prints. Each `[PASS]` is followed by a *sentence*, not a status word —
`chain_integrity` reads "N excerpt entries hash correctly and all N-2 adjacent
pair(s) link …", not `VERIFIED_PARTIAL`. If `chain_store` or
`fingerprint_matches_genesis` reads anything other than a success sentence, the
ledger copy and the report are from different resets and the fallback is
useless.

## What is deliberately not here

**No pre-recorded PhotoRec scan and no pre-recorded carve.** Both beats already
read their numbers from JSON files that `demo-reset.sh` writes, and the runbook's
fallback for both is the hardware validation run in
`docs/validation/results-20260905T033655Z/`, which is real measured data rather
than a recording of a screen.

**Nothing is committed.** These files are large, and the report and ledger carry
a signing key's fingerprint from a scratch state directory. `.gitignore` covers
them; this README is the only tracked file in here.
